#include <bits/stdc++.h>
using namespace std;

int next(int n){
    int digitos = floor(log10(n)) + 1;
    if(n == 0) digitos = 1;

    for(int i = 2; i<=digitos; ++i){
        if(n%(int)pow(10,i) > 1);
    }
}

int main(){
    int n, m, s;
    cin >> n >> m >> s;

    int num;
    
}